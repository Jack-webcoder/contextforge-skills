interface:
  display_name: "Spec"
  short_description: "Write an implementation contract"
  default_prompt: "Turn this approved change into a ready implementation spec with testable acceptance criteria."
policy:
  allow_implicit_invocation: true
