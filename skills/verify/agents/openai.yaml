interface:
  display_name: "Verify"
  short_description: "Prove the changed behavior"
  default_prompt: "Verify this change against its acceptance criteria and write a concrete evidence record."
policy:
  allow_implicit_invocation: true
